import setuptools

# with open('README.md', 'r') as f:
#     long_description = f.read()

setuptools.setup(name='Application Name'
                 version='0.0.1',
                 author='author name',
                 description='some description',
                 packages=setuptools.find_packages(),
                 include_package_data=True)
